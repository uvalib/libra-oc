<% if false %>
<div class="list-group-item">
  <span class="glyphicon glyphicon-time"></span> Joined on <%= user.created_at.to_date.strftime("%b %d, %Y") %>
</div>
<%end%>

<% if Toggles.expose_collections %>
  <div class="list-group-item">
    <span class="badge"><%= number_of_collections(user) %></span>
    <span class="glyphicon glyphicon-folder-open"></span> <%= link_to_field('depositor', user.to_s, t("hyrax.dashboard.stats.collections"), human_readable_type: "Collection") %>
  </div>
<% end %>

<% if Toggles.expose_aggregate_metrics %>
<div class="list-group-item">
  <span class="badge"><%= number_of_works(user) %></span>
  <span class="glyphicon glyphicon-upload"></span> <%= t("hyrax.dashboard.stats.works") %>

  <ul class="views-downloads-dashboard list-unstyled">
      <li><span class="badge badge-optional"><%= user.total_work_views %></span> <%= t("hyrax.dashboard.stats.file_views").pluralize(user.total_file_views) %></li>
      <li><span class="badge badge-optional"><%= user.total_file_downloads %></span> <%= t("hyrax.dashboard.stats.file_downloads").pluralize(user.total_file_downloads) %></li>
  </ul>
</div>
<% end %>

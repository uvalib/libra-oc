<% if Toggles.expose_thumbnail_form_select && f.object.persisted? && f.object.member_ids.present? %>
  <%= render 'form_representative', f: f %>
  <%= render 'form_thumbnail', f: f %>
<% end %>

<%= f.hidden_field :has_existing_files, value: f.object.member_ids.present? %>

